import 'package:flutter/material.dart';

class ManageTasksPage extends StatelessWidget {
  const ManageTasksPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('This is the Manage Tasks Page'),
      ),
    );
  }
}
